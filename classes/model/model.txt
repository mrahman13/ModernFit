the model interacts with the database
it has functions which will be called by view classes to show data to the user, or will be called by the controller which has data from the user to add to the database